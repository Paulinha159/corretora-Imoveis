const prompt = require ("prompt-sync")();

const db =[];

let proxId = 1 ;

const model = () => {
    const nome = prompt ("Nome: "):
    if (nome != ""){
        return {
            id,
            nome,
        }
    }
}
